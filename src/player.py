# Write a class to hold player information, e.g. what room they are in
# currently.
class Player:

   def __init__(self, room, lives):
       self.room = room
       self.lives = lives



def isPlayerAlive(self):
   if (self.lives > 0):

       return True

       #print(f"You are alive, you have {self.lives} lives")
   else:
       return False

